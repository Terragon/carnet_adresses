{% extends 'base.html.twig' %}

{% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="page-header">
            <h1>
                Carnet d'adresses
                <small>{{ contact.lastName }} {{ contact.firstName }}</small>
                <a class="col-md-2 pull-right btn btn-primary" href="{{ path('contact_index') }}">
                    <span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span>
                    Retour à la liste
                </a>
            </h1>
        </div>

        {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input class="col-md-2 pull-right btn btn-success" type="submit" value="Valider" />
        {{ form_end(edit_form) }}

        {{ form_start(delete_form) }}
            <input class="col-md-2 pull-right btn btn-danger delete" type="submit" value="Supprimer">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
